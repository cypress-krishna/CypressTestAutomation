/// <reference types = "cypress"/>

describe('Base Configurations', ()=> {

    it('Launch the url-1', ()=> {
        // cy.visit('https://itera-qa.azurewebsites.net/')
        cy.visit('/')
    });

});
